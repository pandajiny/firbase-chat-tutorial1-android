<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chattutoria1android">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chat.ChatRoom.ChatRoomActivity"></activity>
        <activity android:name=".Friends.FriendsActivity" />
        <activity android:name=".Profile.ProfileEditActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".UserSearch.UserSearchActivity" />
        <activity android:name=".FriendsListActivity" />
        <activity android:name=".ChatListActivity" />
        <activity android:name=".ChatRoomActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".UserActivityy" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>